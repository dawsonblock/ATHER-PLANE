# Remaining work after v2.33

1. Run the controlled fixed-meta-policy vs DREAM-RSI experiment with identical
   initial policy, agent, evaluator, seeds, per-round transition budget, and
   worker budget.
2. Use at least five paired seeds and multiple recursive rounds. Measure
   heldout/transfer success per environment transition and per accelerator-hour.
3. Run the same comparison first in the procedural factorized environment, then
   repeat the surviving configuration on real structured ViZDoom.
4. Preserve complete offered decision menus in every real campaign receipt; do
   not backfill menus after seeing outcomes.
5. Add richer support-distance diagnostics for nearby but non-identical factor
   compositions. Until then, unexecuted decisions remain unsupported.
6. Run the full 25K/100K seven-system ablation and prune mechanisms that fail
   the preregistered keep criteria.
7. Complete real V-JEPA qualification after structured ViZDoom control is
   stable.
8. Do not interpret software closure or the source paper's discovery-domain
   results as proof of embodied-RL improvement. That claim requires the paired
   online experiment above.
